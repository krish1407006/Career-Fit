Django>=5.1,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
psycopg[binary]>=3.2
python-dotenv>=1.0
django-cors-headers>=4.4
Pillow>=10.4
# AI service layer uses the OpenAI-compatible API via httpx (no SDK required)
# External AI provider is called through a service layer to keep provider
# specifics isolated. Kept out of core requirements by design.pypdf>=5.0
python-docx>=1.1
httpx>=0.27
